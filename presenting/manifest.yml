---
applications:
- name: presenting
  memory: 64M
  instances: 1
  host: presenting-${random-word}
  path: build/libs/presenting-1.0.0.jar
  services:
    - rabbitmq
    - discovery
    - config-server
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
---
applications:
- name: config-server
  memory: 64M
  instances: 1
  host: config-server-${random-word}
  path: build/libs/config-server-1.0.0.jar
  services:
    - rabbitmq
    - discovery
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
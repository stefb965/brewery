---
applications:
- name: brewing
  memory: 64M
  instances: 1
  host: brewing-${random-word}
  path: build/libs/brewing-1.0.0.jar
  services:
    - rabbitmq
    - discovery
    - config-server
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"